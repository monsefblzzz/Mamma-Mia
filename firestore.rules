rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
    
    match /orders/{orderId} {
      // Anyone can create an order, read orders, etc for MVP.
      allow read, write: if true;
    }
  }
}
